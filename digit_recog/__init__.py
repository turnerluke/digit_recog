"""Digit recognition: MNIST preprocessing, model, and visualization.

The Streamlit UI in ``streamlit_app/`` and the training script ``train.py`` are
thin layers over the pure logic in this package, which is importable and
testable without a running Streamlit server.
"""
